# StarBuddy BabySteps Web3 architecture

架构源使用 StarBuddy 的奶油纸张、杏色、鼠尾草绿和星空蓝主题。参考作业参考清单、公开同学项目 `Chi111/web3-university`、`yue3694/x-web3` 以及学习资料中的 Worker/Lambda 和 AWS 部署架构图；吸收链上/链下分层、全局架构与业务时序分图、环境化 RPC、移动钱包兼容、证据门禁和私网部署思想，不复制 Foundry、固定 Sale、EC2 单机数据栈、MockUSDC、明文 Worker 私钥或与当前实现不符的服务。

`x-web3` 的 `ChainlinkPriceOracle` 只提供 ETH/USD 参考价与新鲜度校验，项目 README 也明确它不决定 Swap 执行汇率。BabySteps 当前 Chainlink 集成是 VRF v2.5 随机数，不是价格预言机；BABY 兑换使用 Uniswap v3 Quoter/Router 的池内报价。因此架构图不会把 VRF 错标为价格 Oracle，也不会把外部参考价写成结算真相。

状态含义：

- `现有 Sepolia`：已有公开地址、交易或独立读取证据
- `本地已验证`：代码、测试与本地部署通过，但没有外部环境证据
- `远程基础设施已建`：远程资源和独立读取证据存在，但依赖它的应用可能仍受发布门禁限制
- `AWS 云端已验证 / stopped`：真实 Stack、资源读取、Lambda 调用和 RDS 自动停库证据均存在
- `待部署`：部署图或适配代码存在，外部写操作尚未执行
- `计划`：尚未实现

## 系统上下文

```mermaid
%%{init: {"theme":"base","themeVariables":{"primaryColor":"#FFF3D6","primaryTextColor":"#4B2B20","primaryBorderColor":"#9A6548","lineColor":"#496B63","secondaryColor":"#F7B267","tertiaryColor":"#DCE9C8"}}}%%
flowchart LR
    parent0["家长<br/>购买、评论、完成任务"]
    provider0["机构或育婴师<br/>提交成长任务与完成证据"]
    owner0["Owner<br/>角色管理、审核、暂停"]
    babySteps["BabySteps / StarBuddy<br/>React + Worker + Web3"]
    public0["访客<br/>只读任务与公开评论"]

    parent0 --> babySteps
    provider0 --> babySteps
    owner0 --> babySteps
    public0 --> babySteps

    babySteps --> ethereum0["Ethereum Sepolia<br/>可信业务事实"]
    babySteps --> cloudflare0["Cloudflare Pages / Worker / D1<br/>Worker V2 + D1 + HTTPS 已验证"]
    babySteps --> aws0["AWS 可暂停阶段<br/>VPC / 私有 RDS / Readiness + 自动停库 Lambda<br/>云端已验证，RDS stopped"]
    babySteps --> index0["The Graph v0.1.0 已同步<br/>ethers 三 RPC 对照一致"]
```

## 运行时请求与数据流

```mermaid
%%{init: {"theme":"base","themeVariables":{"primaryColor":"#FFF3D6","primaryTextColor":"#4B2B20","primaryBorderColor":"#9A6548","lineColor":"#496B63","secondaryColor":"#F7B267","tertiaryColor":"#DCE9C8"}}}%%
flowchart LR
    parent["家长<br/>外部钱包 + Privy<br/>App ID / 正式域名已配置；登录 UI 待发布验收"] --> web["BabySteps React<br/>现有并扩展"]
    provider["机构或育婴师<br/>V2 提交与审核已在 Sepolia 验证"] --> web
    owner["Owner<br/>现有管理员"] --> web

    web --> worker["Worker API<br/>V2 版本已发布；HTTP/TLS/CORS 已验证"]
    worker --> d1[("D1<br/>metadata、视频、评论、nonce、审计<br/>远程 DB + migrations 已验证")]
    web --> router["Uniswap v3 Router / Quoter<br/>BABY/USDC + BABY/WETH 两池与真实 swap 已验证"]
    web --> marketV1["Marketplace V1<br/>现有 Sepolia 证据"]
    web --> marketV2["Marketplace V2<br/>Sepolia 审核、购买、完成闭环已验证"]
    marketV1 --> coin["BabyCoin<br/>现有 Sepolia"]
    marketV2 --> coin
    marketV2 --> vrf["Chainlink VRF v2.5<br/>V2 随机价格与时长已验证"]
    marketV2 --> sbt["ERC-5192 SBT<br/>Sepolia #1 已铸造且 locked"]
    sbt --> ipfs[("IPFS metadata<br/>CID 已确定，pin 待验证")]
    worker -.->|"生产 Relayer 延后"| lambda["Readiness / 自动停库 Lambda<br/>43 项 AWS 包测试 + 云端调用通过"]
    lambda --> rds[("私有 RDS PostgreSQL<br/>5 分钟自动停库<br/>CREATE_COMPLETE / stopped")]
    kms["AWS KMS secp256k1 Relayer<br/>代码已验证；持续计费阶段延后"] -.-> marketV2
    marketV2 --> graph["The Graph Studio<br/>Sepolia v0.1.0；100% 同步；GraphQL 已读回"]
    marketV2 --> rpc["ethers 三 RPC<br/>Public + Infura + Alchemy<br/>同一交易事实对照一致"]
    web --> keepsakes["StarBuddy 纪念馆<br/>抽卡 / 融合 / 恢复 UI<br/>本地与 Sepolia 抽卡已验证"]
    keepsakes --> notebook["OnchainNotebook<br/>可转送成长星 spend / refund<br/>Sepolia：0xDA7A…CF91"]
    keepsakes --> keepsakeCoordinator["StarBuddyKeepsakes<br/>固定 12 星 / 三卡融合 / 24h recovery<br/>Sepolia：0xb343…bF68"]
    keepsakeCoordinator --> keepsakeVrf["Chainlink VRF v2.5<br/>稀有度 / 阶段 / 融合随机<br/>Sepolia 抽卡已验证"]
    keepsakeCoordinator --> keepsakeSbt["StarBuddyKeepsakeSBT<br/>ERC-5192 · burn 仅限协调合约<br/>Sepolia：0xED65…E627 · SBT #1"]
```

## 链上与链下事实所有权

```mermaid
flowchart TB
    draft["D1 draftId<br/>标题、说明、封面、视频、完成规则<br/>远程公开 API 已验证"]
    canonical["规范化 metadata JSON<br/>keccak256 哈希<br/>Worker / 本地哈希一致"]
    event["TaskRequested event<br/>taskId + metadataHash<br/>Sepolia Task #2 已验证"]
    key["稳定业务键<br/>chainId:marketplace:taskId<br/>远程绑定已验证"]
    task["D1 published_task<br/>保存 key、draftId、txHash、哈希<br/>远程写入已验证"]
    read["详情聚合器<br/>链上状态 + D1 富内容<br/>公开读回已验证"]

    draft --> canonical
    canonical -->|"Provider requestTask"| event
    event -->|"Worker 校验交易回执和 getTask"| key
    draft --> task
    key --> task
    event --> read
    task --> read

    comments[("D1 comments<br/>公开读、购买后写、软隐藏<br/>购买门控写入与公开读回已验证")] --> read
    audit[("D1 audit_log<br/>nonce、绑定、资料、隐藏<br/>远程写入路径已验证")] -.-> task
```

链上拥有 Provider、payee、状态、随机价格/时间、购买、完成和证书事实；D1 拥有富文本、视频 URL、用户名、评论、nonce、会话与审计。任何链上/D1 哈希不一致都返回明确冲突，不自动覆盖。

## 组件、职责、存储与外部服务

```mermaid
flowchart TB
    subgraph browser["浏览器信任边界"]
        ui["React UI<br/>交易状态与只读展示"]
        wallet["Privy / Smart Wallet / 外部钱包<br/>App ID + Allowed origin 已配置；UI 登录待发布验收"]
    end
    subgraph cloudflare["Cloudflare 边界"]
        pages["Pages<br/>现有 Git 集成"]
        worker2["Worker<br/>V2 外部部署 + 健康检查 + CORS 已验证"]
        d12[("D1<br/>远程 DB + migration 已验证")]
    end
    subgraph ethereum["Ethereum Sepolia 边界"]
        baby["BabyCoin<br/>现有：0x108a…5471b"]
        oldMarket["Marketplace V1<br/>现有：0x2D11…25BE"]
    newMarket["Marketplace V2<br/>0x2EE9…15de<br/>Sepolia 完整闭环已验证"]
        cert["ERC-5192<br/>0xF4ef…F654<br/>SBT #1 locked"]
        uni["Uniswap v3<br/>BABY/USDC + BABY/WETH 已注资并完成真实 swap"]
        chainlink["Chainlink VRF v2.5<br/>V1 + V2 真实履约证据"]
        notebook2["OnchainNotebook V2<br/>transferable spend / refund<br/>Sepolia 真实扣除 12 星"]
        keepsakeMarket["StarBuddyKeepsakes<br/>draw / fuse / recovery<br/>真实 VRF 抽卡已验证"]
        keepsakeToken["KeepsakeSBT<br/>series × rarity<br/>SBT #1 locked 已读回"]
    end
    subgraph aws["AWS 当前允许边界（云端已验证，RDS 已 stopped）"]
        probe["Readiness Lambda<br/>直接 invoke 200 / ready"]
        subnets["VPC / 2 AZ / 2 私有子网<br/>无 IGW / NAT / 公网 IP"]
        stop["EventBridge 每 5 分钟<br/>StopDatabase Lambda"]
        db[("RDS PostgreSQL 18.3<br/>Single-AZ / 20 GB gp3 / 无 ingress<br/>private / encrypted / stopped")]
    end
    subgraph deferred["AWS 持续计费边界（延后，当前流水线无权限）"]
        relayer["生产 Completion Relayer"]
        nat["NAT + EIP / 公网 IPv4"]
        secrets["Secrets Manager"]
        key["KMS ECC_SECG_P256K1"]
    end

    pages --> ui
    wallet --> ui
    ui --> worker2 --> d12
    ui --> oldMarket
    ui --> newMarket
    ui --> uni
    newMarket --> baby
    newMarket --> cert
    newMarket --> chainlink
    ui --> keepsakeMarket
    keepsakeMarket --> notebook2
    keepsakeMarket --> chainlink
    keepsakeMarket --> keepsakeToken
    worker2 -.-> relayer
    subnets --> db
    stop --> db
    relayer -.-> db
    relayer --> secrets
    relayer --> key
    relayer -.-> nat -.-> newMarket
    key -.-> newMarket
```

## 登录、写入与购买校验时序

Worker/D1 公开 API 已验证以下时序，并已绑定 Sepolia Task #2；Privy App ID 与正式 Allowed origin 已配置，邮箱/Google 登录 UI 仍待本批发布后验收。闭环签名由测试部署钱包真实执行，Evidence 不保存签名、Cookie 或私钥。

```mermaid
sequenceDiagram
    participant U as 家长钱包 / Privy
    participant UI as React
    participant W as Worker
    participant D as D1
    participant M as Marketplace V2

    U->>UI: 邮箱嵌入式钱包或外部钱包
    UI->>W: 请求 challenge(wallet, action)
    W->>D: 写入加密随机 nonce + 过期时间
    W-->>UI: EIP-4361 风格待签消息
    UI->>U: personal_sign
    UI->>W: message + signature
    W->>D: 原子消费 nonce
    W->>W: 校验 domain、chainId、wallet、action、expiry
    W-->>UI: HttpOnly 短期会话

    UI->>W: 发布评论(taskKey, content)
    W->>M: purchaseIdForBuyer(taskId, wallet)
    M-->>W: purchaseId 或 0
    alt 已购买且会话有效
        W->>D: 写评论与审计
        W-->>UI: 201 Created
    else 未购买、nonce 重放或会话过期
        W-->>UI: 401/403 + 稳定错误码
    end
```

## 部署与 CI/CD

```mermaid
flowchart LR
    feature["Feature branch"] --> local["本地门禁<br/>全仓测试、类型、构建、敏感信息"]
    feature --> preview["Cloudflare Pages branch preview<br/>Git 集成；URL/产物验收"]
    preview --> previewLife["PR 关闭后的清理/保留周期<br/>尚未自动化，待验证"]
    local --> main["main（合并仍需人工决定）"]
    main --> manual["workflow_dispatch<br/>start_services=true"]
    manual --> env["GitHub Environment<br/>aws-readiness 人工审批"]
    env --> oidc["GitHub OIDC<br/>短期 AWS 身份<br/>无 Access Key"]
    oidc --> s3["加密 S3 source/{commit}.zip<br/>7 天生命周期"]
    s3 --> codebuild["CodeBuild Linux/Small<br/>并发上限 1"]
    codebuild --> checks["43 AWS tests + typecheck<br/>bootstrap/pausable SAM lint + 权限负向测试"]
    checks -->|"ALLOW_AWS_PAUSABLE_DEPLOYMENT=true"| awsDeploy["SAM / CloudFormation<br/>VPC、私有 RDS、Readiness / 自动停库 Lambda"]
    checks -. "无权限且 buildspec 拒绝" .-> persistent["NAT / EIP / KMS / Secrets / 生产 Relayer<br/>持续计费，等待再次确认"]
    awsDeploy --> evidence["Lambda 200 / 私网路由 / RDS stopped<br/>脱敏 Evidence"]
    evidence --> cleanupGate["费用复核与 ExpiresAt<br/>触发清理检查"]
    cleanupGate --> reversible["RDS 自动停止<br/>VPC/Lambda/Rule 保留；存储仍计费"]
    cleanupGate --> deleteApproval["最终删除 Stack<br/>复盘后确认精确目标"]
    main --> pagesDeploy["Cloudflare Pages Git 部署<br/>现有"]
    main --> workerDeploy["D1 migration + Worker V2 已完成<br/>自定义域名已绑定"]
    main --> graphDeploy["Subgraph build/test + Studio 部署<br/>v0.1.0 已 100% 同步"]
    pagesDeploy --> httpCheck["HTTP、TLS、深链、非空产物<br/>现有门禁"]
    workerDeploy --> apiCheck["HTTP 200、TLS、允许/拒绝 Origin CORS<br/>远程 D1 只读核验"]
    awsDeploy --> awsCheck["IAM 最小权限、Lambda 200、RDS 自动停库<br/>云端已验证"]
```

## 权限与安全边界

```mermaid
flowchart LR
    owner2["Owner<br/>授予 Provider、审核、暂停"] --> market["Marketplace V2<br/>权限、随机、结算与 SBT 已在 Sepolia 验证"]
    provider2["Provider<br/>提交任务与完成证据"] --> market
    provider2 --> api["Worker API<br/>外部已部署；真实签名、会话、D1 写入已验收"]
    parent2["Parent<br/>Swap、Approve、Buy"] --> market
    relayer2["KMS Relayer<br/>本地签名与幂等已验证；持续计费阶段延后<br/>未来仅 COMPLETION_RELAYER_ROLE"] -.-> market
    coordinator["VRF Coordinator<br/>仅随机回调"] --> market
    market --> sbt2["SBT<br/>Marketplace 持 MINTER_ROLE"]

    owner2 -. "不能代替家长支付" .-> parent2
    provider2 -. "不能审核自己的任务" .-> owner2
    relayer2 -. "不能授予角色或暂停" .-> owner2
```

安全约束：Worker 只能通过 D1 binding 访问数据库，不调用 Cloudflare 管理 REST API；请求级状态不得保存在模块全局变量；nonce 和会话令牌只保存 SHA-256 哈希；评论写入前重新读取链上 `purchaseIdForBuyer`。AWS 可暂停 Stack 已真实验证没有 IGW/NAT/公网 IP，RDS 无 ingress 且为 stopped；EC2 仍保留默认 SG egress，但路由表只有 local，因此当前没有实际公网出口，未来新增连接前必须收紧。密码由 CodeBuild 运行时生成并通过 CloudFormation `NoEcho` 参数传递；自动停库 Lambda 只能描述并停止目标 DB。GitHub OIDC 与角色已建，main Actions 触发仍待验证；执行角色无 NAT/EIP/KMS/Secrets/API Gateway 创建权限。完整 Worker→Lambda HMAC、KMS EIP-1559 signer 与 RDS 幂等代码仍保留为后续参考，但当前流水线不能部署。

## 预览环境生命周期与清理

Cloudflare Pages 的 feature/PR 构建只能作为预览，必须在合并前验证非空产物、deployment-specific URL、canonical pages.dev alias、自定义域名、TLS 和产品深链。PR 关闭后，清理逻辑只能按该 PR 的 `preview_id` 处理临时别名或临时资源；不能删除 Pages 正式项目、共享域名、共享 GitHub OIDC、共享 artifact bucket、BabySteps VPC 或 RDS。

AWS 当前没有自动创建每 PR 重资源。可暂停 Stack 仅能通过人工批准的 `workflow_dispatch` 和费用 Gate 启动；RDS 由 EventBridge 自动停止，但存储仍会计费。最终删除 Stack 属于破坏性操作，必须重新只读盘点账号、Region、Stack 和受保护共享资源后获得明确授权。任何清理失败都应停止并记录，而不是扩大删除范围。

## 边缘 SSR、水合与 CSR 降级

BabySteps 不再用 HashRouter 模拟页面，而是以真实 URL 和 History API 建立九条产品路由。最终组合是 `Cloudflare Edge SSR + React hydration + 客户端交互激活 + 纯 CSR fallback`，不是把 Vite CSR 冒充 SSR，也不把前端 SSR 复制到 AWS。

- 可服务端渲染：公开首页、公开路由骨架、Evidence 说明、导航、页脚与不依赖钱包的确定性内容。
- 只能客户端执行：Privy、wagmi、钱包余额、链上交易、个人资料、用户会话和性能 SDK。
- 不能序列化：钱包地址对应的私有资料、身份令牌、Cookie、Authorization、交易签名与请求正文。
- 本地已验证：双端生产构建、公开页 SSR、client-shell SSR、真实 404、静态资源直通、SSR 异常/超时 fallback、致命水合失败最多一次 CSR 重挂载。
- 已验证：PR #21 预览、deployment-specific URL、pages.dev、自定义域名、TLS、真实深链、SSR 响应头与生产别名；main 合并提交 `91dcc4c`，GitHub Run `31789478284`，Cloudflare deployment `5f4a39e0-0fc5-4bd2-87a2-25158fe2111b`。
- 共享反馈：通用 standard、静态/Edge SSR 策略、TC Flow N6、reusable workflow 与旧 SSG 项目已完成 68/68 回归，并由共享提交 `0c9185f`、PR #14、Run `31791228753` 发布；BabySteps 已声明 edge-ssr、rendering manifest、非空 `_worker.js` 和独立 built Worker 运行矩阵。项目 PR #23、Run `31791893461`、Preview deployment `23e11aa6-5e04-4aa0-ba79-6fd3b66dc1f4` 的三项 Gate、SSR、深链、404、资源直通和 TLS 均已验证。
- 费用：本次渲染优化没有 AWS 写操作或新资源，AWS 增量成本为 `$0`；既有共享 VPC/NAT/RDS/OIDC 仍受保护。

```mermaid
flowchart LR
    browserR["浏览器<br/>真实 URL / History API"] --> edgeR["Cloudflare Pages Edge<br/>请求分类"]
    edgeR -->|"HTML GET / HEAD"| ssrR["React Web Streams SSR<br/>StaticRouter + ServerRouteShell"]
    edgeR -->|"assets / API / mutation"| assetsR["ASSETS.fetch<br/>静态资源直通"]
    ssrR --> stateR["安全状态白名单<br/>mode / pathname / version"]
    stateR --> hydrateR["BrowserRouter + hydrateRoot<br/>SSR 标记 / 路径 / 版本一致"]
    hydrateR --> clientR["客户端激活<br/>Privy / wagmi / Web3 / SDK"]
    ssrR -. "超时 / 异常" .-> fallbackR["静态 index.html<br/>纯 CSR createRoot"]
    hydrateR -. "致命失败" .-> fallbackR
    actionsR["GitHub Actions<br/>双端构建 + route manifest Gate"] --> edgeR
    clientR --> perfR["既有性能链路<br/>Worker → AWS；本次无 AWS 写入"]
```

```mermaid
sequenceDiagram
    participant B as 浏览器
    participant E as Pages Edge
    participant P as Request Policy
    participant R as React SSR
    participant A as 静态 Assets
    participant C as Client App

    B->>E: GET /tasks + Accept:text/html
    E->>P: 分类请求与路由状态
    P->>R: pathname + render policy
    R-->>E: 完整读流后返回 HTML + 安全状态
    E-->>B: 200/404 + render-mode:ssr
    B->>C: SSR 标记、路径、版本一致后 hydrateRoot
    C->>C: 激活 Privy/wagmi/性能 SDK

    alt SSR 超时或异常
        R--xE: abort / render error
        E->>A: 读取静态 index.html
        A-->>B: csr-fallback + no-store
        B->>C: createRoot 启动纯 CSR
    else 水合致命失败
        C->>C: unmount + 清空 root
        C->>C: 最多一次 CSR 重挂载
    end
```

### 应反向优化的共享能力

以下检测是 BabySteps 真实实现后可泛化的能力，适合反馈到 shared standard、TC Flow 本地 Gate 与 GitHub Actions 远端 Gate；项目专属的 Privy、wagmi、StarBuddy 路由名称不得写入通用规则。

1. route manifest 与实际路由、深链和 404 状态码一致；
2. SSR 声明必须有非空服务端产物、真实 SSR 响应标记和资源直通证明；
3. 服务端状态只能使用显式白名单，序列化必须防止 HTML/script 注入；
4. 水合只允许在 SSR 标记、请求路径与构建版本匹配时发生；React 报告可恢复 DOM 差异，致命失败最多一次 CSR 重挂载；
5. 服务端 bundle 不得包含浏览器钱包/身份 SDK 的运行时代码；
6. Gate 必须直接执行构建后的 Worker，覆盖 canonical 路由、尾斜杠、真实 404、API 直通和 client-shell 缓存策略；
7. 交付 `base` 检查必须由 delivery contract 决定：Pages Advanced Worker + History Router 使用 `/`，明确 static-only 的项目才允许 `./`；
8. Dashboard 的准确标签为：`rendering: edge-ssr`、`client activation: hydration`、`fallback: pure-csr`、`wallet/identity: client-only`、`cloud production: verified`。

反馈闭环状态：BabySteps 的“项目发现 → 泛化判断”已完成并本地验证；回写共享 standard、检测脚本、TC Flow、GitHub Actions 和旧项目回归由共享方案任务接续，未完成前不得标记为已发布。

## StarBuddy 纪念卡抽取、融合与恢复时序

本段是产品增强，不属于老师七项课程平台硬性要求。Sepolia 三合约、VRF consumer、固定 12 星真实抽卡和锁定 SBT #1 已验证；真实融合仍等待自然积累三张同系列、同稀有度卡，图中不把本地融合测试冒充链上交易。

```mermaid
sequenceDiagram
    participant P as 家长钱包
    participant UI as 纪念馆 UI
    participant N as OnchainNotebook
    participant K as StarBuddyKeepsakes
    participant V as Chainlink VRF v2.5
    participant S as KeepsakeSBT

    P->>UI: 发起固定 12 成长星抽卡
    UI->>K: requestDraw()
    K->>N: spendTransferable(parent, 12)
    K->>V: requestRandomWords()
    V-->>K: fulfillRandomWords(series, rarity)
    K->>S: mint(parent, series, rarity)
    S-->>UI: ERC-5192 token + deterministic metadata URI

    P->>UI: 选择 3 张同系列、同稀有度卡
    UI->>K: requestFusion(tokenIds)
    K->>S: lock 3 parents
    K->>V: requestRandomWords()
    alt 融合成功
        V-->>K: 100/70/40% 命中
        K->>S: burn 3 + mint next rarity
    else 融合失败
        V-->>K: 随机选择被烧毁 parent
        K->>S: burn 1 + unlock 2
    end
    alt 24 小时未回调
        P->>K: recoverRequest(requestId)
        K->>N: 抽卡退款 / 融合解锁
        V-->>K: 迟到回调被忽略
    end
```

## 失败恢复与 Evidence

```mermaid
flowchart LR
    action["关键操作<br/>迁移、绑定、部署、交易"] --> checkpoint["写 checkpoint<br/>idempotencyKey / txHash / deploymentId"]
    checkpoint --> verify["独立读取验证<br/>D1、RPC、HTTP、TLS、GraphQL"]
    verify -->|"一致"| evidence2["脱敏 Evidence<br/>代码行、测试、哈希、截图"]
    verify -->|"暂时失败"| retry["有界重试<br/>不重复扣款或铸造"]
    verify -->|"配置/权限错误"| stop["停止门禁<br/>标记 blocked，不继续生产"]
    retry --> verify
    evidence2 --> map["作业实现映射<br/>更新真实状态"]
    map --> cost["费用与资源清单<br/>当前：RDS storage / CodeBuild 请求<br/>延后：NAT / EIP / KMS / Secrets"]
    cost --> cleanup["触发 aws-delivery-cleanup<br/>可逆停用优先"]
    cleanup -->|"最终删除前"| confirm["复盘后确认 stack、region、资源列表"]
```

## 当前图像证据

公开 Evidence 当前使用 `starbuddy-web3-global-architecture.svg` 与 `starbuddy-web3-business-sequence.svg`：前者以六列责任边界和四条数据带展开用户运行、链上/链下事实、Sepolia 资产闭环、外部依赖、交付/AWS、失败与清理；后者按登录会话、Uniswap 获币、Provider 上架与 Owner 审核、家长购买结算、成长任务完成与证书、纪念卡抽取与融合六段展开成功主线与失败降级。`starbuddy-web3-architecture.png` 与 `starbuddy-web3-architecture-v2.png` 仅保留为早期 StarBuddy 产品视图，不再代表当前工程状态。本 Mermaid 仍是包含当前部署状态、CI/CD、安全与清理边界的工程真相源；任何图片不得覆盖这里的真实状态。

V2 合约与完整 Sepolia 业务闭环记录在 `docs/evidence/deployment/2026-08-11-sepolia-v2-business.json`；Worker V2、自定义域名、D1、HTTP/TLS/CORS 记录在 `docs/evidence/deployment/2026-08-11-cloudflare-worker-v2.json`；公开 API 的签名会话、Task #2、购买、用户名与评论闭环记录在 `docs/evidence/deployment/2026-08-12-public-api-closed-loop.json`；Uniswap 两池与 swap 记录在 `docs/evidence/deployment/2026-08-12-uniswap-v3-verification.json`；The Graph 与三 RPC 分别记录在 `docs/evidence/deployment/2026-08-12-the-graph-sepolia.json` 和 `2026-08-12-rpc-comparison.json`。仍待的硬性外部边界只有本批发布后的 Privy 登录 UI；IPFS pin 为增强项。AWS 本地门禁见 `docs/evidence/testing/2026-08-10-aws-readiness-local.md`，真实云端部署、失败恢复、网络与停库证据见 `docs/evidence/deployment/2026-08-11-aws-pausable.md`。
