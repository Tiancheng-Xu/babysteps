# StarBuddy BabySteps Web3 architecture

架构源使用 StarBuddy 的奶油纸张、杏色、鼠尾草绿和星空蓝主题。参考 `HOMEWORKS.md`、公开同学项目 `Chi111/web3-university` 以及学习资料中的 Worker/Lambda 和 AWS 部署架构图，但不复制其 MockUSDC、私钥执行器、PinMe 或 RDS/VPC 复杂度。

状态含义：

- `现有 Sepolia`：已有公开地址、交易或独立读取证据
- `本地已验证`：代码、测试与本地部署通过，但没有外部环境证据
- `待部署`：部署图或适配代码存在，外部写操作尚未执行
- `计划`：尚未实现

## 系统上下文

```mermaid
%%{init: {"theme":"base","themeVariables":{"primaryColor":"#FFF3D6","primaryTextColor":"#4B2B20","primaryBorderColor":"#9A6548","lineColor":"#496B63","secondaryColor":"#F7B267","tertiaryColor":"#DCE9C8"}}}%%
flowchart LR
    parent0["家长<br/>购买、评论、完成任务"]
    provider0["机构或育婴师<br/>提交成长任务与完成证据"]
    owner0["Owner<br/>角色管理、审核、暂停"]
    babySteps["BabySteps / StarBuddy<br/>React + Worker + Web3"]
    public0["访客<br/>只读任务与公开评论"]

    parent0 --> babySteps
    provider0 --> babySteps
    owner0 --> babySteps
    public0 --> babySteps

    babySteps --> ethereum0["Ethereum Sepolia<br/>可信业务事实"]
    babySteps --> cloudflare0["Cloudflare Pages / Worker / D1<br/>界面、API、富内容"]
    babySteps --> aws0["AWS Lambda / KMS<br/>受控完成签名"]
    babySteps --> index0["The Graph + 3 RPC<br/>读取与交叉验证"]
```

## 运行时请求与数据流

```mermaid
%%{init: {"theme":"base","themeVariables":{"primaryColor":"#FFF3D6","primaryTextColor":"#4B2B20","primaryBorderColor":"#9A6548","lineColor":"#496B63","secondaryColor":"#F7B267","tertiaryColor":"#DCE9C8"}}}%%
flowchart LR
    parent["家长<br/>现有：外部钱包<br/>计划：Privy 邮箱"] --> web["BabySteps React<br/>现有并扩展"]
    provider["机构或育婴师<br/>现有 V2 审核代码，待部署"] --> web
    owner["Owner<br/>现有管理员"] --> web

    web --> worker["Worker API<br/>本地已验证，待部署"]
    worker --> d1[("D1<br/>metadata、视频、评论、nonce、审计<br/>本地已验证，待部署")]
    web --> router["Uniswap v3 Router<br/>计划，待验证"]
    web --> marketV1["Marketplace V1<br/>现有 Sepolia 证据"]
    web --> marketV2["Marketplace V2<br/>现有审核、购买与幂等完成代码<br/>待验证部署"]
    marketV1 --> coin["BabyCoin<br/>现有 Sepolia"]
    marketV2 --> coin
    marketV2 --> vrf["Chainlink VRF v2.5<br/>现有能力，V2 待验证"]
    marketV2 --> sbt["ERC-5192 SBT<br/>现有代码，待验证部署"]
    sbt --> ipfs[("IPFS metadata<br/>计划，待验证")]
    worker --> lambda["Lambda Relayer<br/>计划"]
    lambda --> kms["AWS KMS<br/>计划，待验证"]
    kms -->|"COMPLETION_RELAYER_ROLE<br/>代码现有，云端待验证"| marketV2
    marketV2 --> graph["The Graph<br/>计划，待验证"]
    marketV2 --> rpc["公共 RPC、Infura、Alchemy<br/>计划三源对照"]
```

## 链上与链下事实所有权

```mermaid
flowchart TB
    draft["D1 draftId<br/>标题、说明、封面、视频、完成规则<br/>本地已验证"]
    canonical["规范化 metadata JSON<br/>keccak256 哈希<br/>本地已验证"]
    event["TaskRequested event<br/>taskId + metadataHash<br/>本地已验证合约"]
    key["稳定业务键<br/>chainId:marketplace:taskId<br/>本地已验证"]
    task["D1 published_task<br/>保存 key、draftId、txHash、哈希<br/>本地已验证"]
    read["详情聚合器<br/>链上状态 + D1 富内容<br/>本地已验证"]

    draft --> canonical
    canonical -->|"Provider requestTask"| event
    event -->|"Worker 校验交易回执和 getTask"| key
    draft --> task
    key --> task
    event --> read
    task --> read

    comments[("D1 comments<br/>公开读、购买后写、软隐藏<br/>本地已验证")] --> read
    audit[("D1 audit_log<br/>nonce、绑定、资料、隐藏<br/>本地已验证")] -.-> task
```

链上拥有 Provider、payee、状态、随机价格/时间、购买、完成和证书事实；D1 拥有富文本、视频 URL、用户名、评论、nonce、会话与审计。任何链上/D1 哈希不一致都返回明确冲突，不自动覆盖。

## 组件、职责、存储与外部服务

```mermaid
flowchart TB
    subgraph browser["浏览器信任边界"]
        ui["React UI<br/>交易状态与只读展示"]
        wallet["Privy 或外部钱包<br/>计划"]
    end
    subgraph cloudflare["Cloudflare 边界"]
        pages["Pages<br/>现有 Git 集成"]
        worker2["Worker<br/>本地已验证鉴权、聚合与 API<br/>外部待部署"]
        d12[("D1<br/>本地已验证富内容、会话、审计<br/>外部待部署")]
    end
    subgraph ethereum["Ethereum Sepolia 边界"]
        baby["BabyCoin<br/>现有：0x108a…5471b"]
        oldMarket["Marketplace V1<br/>现有：0x2D11…25BE"]
        newMarket["Marketplace V2<br/>现有审核、购买、完成代码<br/>待验证部署"]
        cert["ERC-5192<br/>现有代码，待验证部署"]
        uni["Uniswap v3<br/>待验证"]
        chainlink["Chainlink VRF<br/>现有闭环证据"]
    end
    subgraph aws["AWS 签名边界"]
        relayer["Lambda<br/>计划最小权限"]
        key["KMS secp256k1<br/>计划不可导出"]
    end

    pages --> ui
    wallet --> ui
    ui --> worker2 --> d12
    ui --> oldMarket
    ui --> newMarket
    ui --> uni
    newMarket --> baby
    newMarket --> cert
    newMarket --> chainlink
    worker2 --> relayer --> key --> newMarket
```

## 登录、写入与购买校验时序

Worker/D1 本地已验证以下时序；Privy 邮箱入口与外部部署仍是计划，当前自动化测试使用运行时生成的测试钱包签名。

```mermaid
sequenceDiagram
    participant U as 家长钱包 / Privy
    participant UI as React
    participant W as Worker
    participant D as D1
    participant M as Marketplace V2

    U->>UI: 邮箱嵌入式钱包或外部钱包
    UI->>W: 请求 challenge(wallet, action)
    W->>D: 写入加密随机 nonce + 过期时间
    W-->>UI: EIP-4361 风格待签消息
    UI->>U: personal_sign
    UI->>W: message + signature
    W->>D: 原子消费 nonce
    W->>W: 校验 domain、chainId、wallet、action、expiry
    W-->>UI: HttpOnly 短期会话

    UI->>W: 发布评论(taskKey, content)
    W->>M: purchaseIdForBuyer(taskId, wallet)
    M-->>W: purchaseId 或 0
    alt 已购买且会话有效
        W->>D: 写评论与审计
        W-->>UI: 201 Created
    else 未购买、nonce 重放或会话过期
        W-->>UI: 401/403 + 稳定错误码
    end
```

## 部署与 CI/CD

```mermaid
flowchart LR
    feature["Feature branch<br/>现有"] --> local["本地门禁<br/>Worker 47 测试、类型、dry-run、D1 migrations<br/>本地已验证"]
    local --> preview["Preview<br/>独立 Worker + D1 + Pages<br/>计划"]
    preview --> evidence["Evidence gate<br/>现有规则，持续更新"]
    evidence --> approval["人工授权<br/>生产与付费资源"]
    approval --> main["main"]
    main --> pagesDeploy["Cloudflare Pages Git 部署<br/>现有"]
    main --> workerDeploy["Worker/D1 migration<br/>计划"]
    main --> awsDeploy["Lambda/KMS IaC<br/>计划"]
    main --> graphDeploy["Subgraph Studio<br/>计划，待验证"]
    pagesDeploy --> httpCheck["HTTP、TLS、深链、非空产物<br/>现有门禁"]
    workerDeploy --> apiCheck["API 与 migration 验收<br/>计划"]
    awsDeploy --> kmsCheck["IAM、KMS、幂等验收<br/>计划"]
```

## 权限与安全边界

```mermaid
flowchart LR
    owner2["Owner<br/>授予 Provider、审核、暂停"] --> market["Marketplace V2<br/>权限与结算代码现有<br/>部署待验证"]
    provider2["Provider<br/>提交任务与完成证据"] --> market
    provider2 --> api["Worker API<br/>challenge-sign-verify 本地已验证<br/>外部待部署"]
    parent2["Parent<br/>Swap、Approve、Buy"] --> market
    relayer2["KMS Relayer<br/>云端计划<br/>仅 COMPLETION_RELAYER_ROLE"] --> market
    coordinator["VRF Coordinator<br/>仅随机回调"] --> market
    market --> sbt2["SBT<br/>Marketplace 持 MINTER_ROLE"]

    owner2 -. "不能代替家长支付" .-> parent2
    provider2 -. "不能审核自己的任务" .-> owner2
    relayer2 -. "不能授予角色或暂停" .-> owner2
```

安全约束：Worker 只能通过 D1 binding 访问数据库，不调用 Cloudflare 管理 REST API；请求级状态不得保存在模块全局变量；nonce 和会话令牌使用 Web Crypto 且 D1 只保存 SHA-256 哈希；nonce 通过条件更新原子消费；评论写入前重新读取链上 `purchaseIdForBuyer`；所有异步工作必须 await 或进入明确的 `waitUntil`/后续 Queue 边界。Lambda 只允许指定 KMS key 的 `Sign`，KMS 钱包只拥有 `COMPLETION_RELAYER_ROLE`。

## 失败恢复与 Evidence

```mermaid
flowchart LR
    action["关键操作<br/>迁移、绑定、部署、交易"] --> checkpoint["写 checkpoint<br/>idempotencyKey / txHash / deploymentId"]
    checkpoint --> verify["独立读取验证<br/>D1、RPC、HTTP、TLS、GraphQL"]
    verify -->|"一致"| evidence2["脱敏 Evidence<br/>代码行、测试、哈希、截图"]
    verify -->|"暂时失败"| retry["有界重试<br/>不重复扣款或铸造"]
    verify -->|"配置/权限错误"| stop["停止门禁<br/>标记 blocked，不继续生产"]
    retry --> verify
    evidence2 --> map["作业实现映射<br/>更新真实状态"]
```

## 当前图像证据

`docs/architecture/starbuddy-web3-architecture.png` 是现有 StarBuddy 主题图。最终图像依据本 Mermaid 真相源和 StarBuddy 四阶段形象重新生成；未实现节点继续标注“计划”或“待部署”。

Phase 1 的合约、部署图、测试与未部署边界已记录在 `docs/evidence/testing/2026-08-10-web3-v2-contracts.md`；Worker/D1 本地实现记录在 `docs/evidence/testing/2026-08-10-worker-d1.md`。本 Mermaid 是当前架构真相源；PNG 在按此源重新生成前只作为历史主题证据。
