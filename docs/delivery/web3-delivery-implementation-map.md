# BabySteps Web3 作业实现映射

本表以老师公告的 7 项课程平台要求，加上 RPC/The Graph/AWS 部署要求为验收基准。`complete` 仅表示代码、测试和题目要求的外部证据均存在；`partial` 表示实现已存在但仍缺外部配置、资金或部署证据；`pending` 表示尚未实现；`blocked` 表示验证失败并停止。老师公告的 1–7 项按最低验收线判定；新版 UI 再发送一轮同类交易属于产品增强复核，不重复降低已经存在真实链上/云端证据的作业状态。

| 作业要求 | 实现功能 | 代码位置 | 验证证据 | 当前状态 |
| --- | --- | --- | --- | --- |
| 1. 链上任务列表 + 链下数据库，按 ID 绑定视频和评论 | `chainId:marketplaceAddress:taskId` 稳定键；canonical metadata hash；Worker 同时核验回执、`TaskRequested` 和 `getTask`；公开详情隐藏视频与完成说明，已登录且链上已购的钱包才可解锁；评论同样购买门控 | `worker/src/domain/taskMetadata.ts`<br>`worker/src/chain/viemMarketplaceReader.ts`<br>`worker/src/routes/tasks.ts`<br>`worker/src/routes/comments.ts`<br>`web/src/features/marketplace/TaskLearningPanel.tsx` | V2 任务 #2 已完成 D1/链上 ID 绑定、公开任务读取、购买记录与评论闭环；Pages、Worker #4、D1 `0001`–`0003`、健康/鉴权/CORS 均已验证。新版已购内容接口再读回属于产品增强复核，不影响老师要求的链上+链下绑定验收 | `complete` |
| 2. Owner 管理商家/老师，Provider 提交任务并由 Owner 审核上架 | `PROVIDER_ROLE`；Provider UI 调用 V2 `requestTask` 并提交 D1 metadata hash；Owner UI 校验 `DEFAULT_ADMIN_ROLE` 后调用 `approveTask/rejectTask`；`PendingReview → PendingRandomness → Active` | `contracts/contracts/TaskMarketplaceV2.sol`<br>`web/src/features/provider/useProviderTaskCreation.ts`<br>`web/src/features/provider/useOwnerTaskReview.ts`<br>`web/src/pages/ProviderConsolePage.tsx` | Sepolia Provider 请求、Owner 批准与 VRF 激活已有真实任务 #1；V2 页面路径专项测试通过并已发布。使用新版 UI 再发一笔同类交易属于产品增强复核 | `complete` |
| 3. 发行 ERC-20 平台币 | 使用 BabyCoin 取代旧平台代币；原 UI 星星直接改为 BabyCoin 展示，奖励余额与累计成长值分离 | `contracts/contracts/BabyCoin.sol:7`<br>`contracts/contracts/GrowthActivities.sol:1`<br>`web/src/contracts/web3Contracts.ts:1` | Sepolia BabyCoin `0x108a55217011983b93C3A95aD8D3B3343Bd5471b`；合约测试和 V1 闭环 Evidence | `complete` |
| 4. Uniswap 池：USDC、ETH/WETH → 平台币 | 锁定 Uniswap v3 Sepolia 官方 Factory/PositionManager/Router/Quoter、Circle 官方测试 USDC 与官方 WETH9；先用既有 WETH/USDC 池取得官方测试 USDC，再创建 BABY/USDC 与 BABY/WETH；不复制 MockUSDC | `web/src/contracts/web3Contracts.ts:3`<br>`contracts/scripts/provisionSepoliaUniswapV3.ts:23`<br>`contracts/scripts/lib/uniswapPoolMath.ts:1` | BABY/USDC `0x50F6…A9cB`、LP NFT #230840；BABY/WETH `0x4820…Ed80`、LP NFT #230841；15/15 receipts 成功；见 `docs/evidence/deployment/2026-08-12-uniswap-v3-verification.json` | `complete` |
| 4a. 前端按 USDC 或 ETH/WETH 兑换 BabyCoin | QuoterV2 报价；ETH 缺额先 wrap WETH；有限额度 approve；SwapRouter02 exact-input swap；1% 滑点保护和完整状态反馈 | `web/src/features/exchange/useUniswapSwap.ts:69`<br>`web/src/features/exchange/useUniswapSwap.ts:125`<br>`web/src/features/exchange/useUniswapSwap.ts:152`<br>`web/src/features/exchange/useUniswapSwap.ts:171` | 真实 swap：0.1 USDC → 0.0980703969 BABY；0.00005 WETH → 0.0980703969 BABY；均高于 0.095 BABY 最小到账，Router allowance 均为 0 | `complete` |
| 5. 点击购买，精确价格、approve → buy、`msg.sender` 与购买记录写链 | 精确 allowance；`safeTransferFrom(msg.sender)`；每位家长每项任务仅一次；显示购买、暂停、过期和重复失败状态 | `contracts/contracts/TaskMarketplaceV2.sol:254`<br>`web/src/features/marketplace/useTaskPurchase.ts:1` | V2 Sepolia 随机价格为 2 BABY；exact approve `0x3b08…593b`、buy `0x1aaf…302`；Provider 收款 2 BABY，剩余 allowance 为 0 | `complete` |
| 6. Chainlink 随机性；成长任务完成后发行带名称和图的 NFT/SBT | Chainlink VRF v2.5 随机价格与开放时间；家长提交任务完成证据，Worker 用链上购买事实校验并按 `purchaseId` 幂等保存；Owner/受限钱包把同一 evidence hash 传给 `confirmCompletion`，合约铸 ERC-5192 SBT | `contracts/contracts/TaskMarketplaceV2.sol`<br>`contracts/contracts/GrowthCertificateSBT.sol`<br>`worker/src/routes/completions.ts`<br>`worker/src/repositories/completionRepository.ts`<br>`web/src/features/provider/OwnerCompletionReviewPanel.tsx` | Sepolia 已完成真实 VRF、`confirmCompletion` 交易与锁定 SBT #1；证书 URI 含名称和图，D1 `0003` 与 Worker #4 已上线。新版 D1 completion UI 再提交一笔属于产品增强复核 | `complete` |
| 7. 个人中心使用 Privy 登录，可修改用户名并签名 | Privy Google/邮箱/外部钱包；Smart Wallet 懒创建；challenge-sign-verify；12 小时 HttpOnly 会话；D1 用户名；无私钥或 paymaster | `web/src/config/providers.tsx:27`<br>`web/src/features/identity/PrivyIdentityPanel.tsx:1`<br>`worker/src/routes/auth.ts:41`<br>`worker/src/routes/profile.ts:46` | 生产 Privy App ID 与 Allowed origin 已配置；真实 challenge-sign-verify、HttpOnly 会话和 D1 用户名写入已验证。PR #24 修复 `PUT /api/profile` CORS 并锁定预检测试；Google 登录与钱包签名入口已在正式站可用 | `complete` |
| ethers.js 通过公共 RPC、Infura、Alchemy 对照读取链上数据 | 同一交易、回执、区块、余额和链 ID 的三源读取与一致性报告；端点只通过 0600 临时文件注入，输出自动脱敏 | `contracts/scripts/readSepoliaAcrossProviders.ts:12`<br>`contracts/scripts/lib/rpcComparison.ts:163` | 公共 Sepolia、Infura Core、Alchemy Free 三源均读取交易 `0xba0d…02cfd` 成功；chainId、余额、交易、回执和日志完全一致，`complete: true`；证据见 `2026-08-12-rpc-comparison.json` | `complete` |
| The Graph 通过日志索引并用 GraphQL 读回 | schema、V2 ABI、event handlers、Matchstick 测试和 delivery query；索引角色、任务、审核、随机、购买与完成事件 | `subgraph/subgraph.yaml:5`<br>`subgraph/src/task-marketplace.ts:58`<br>`subgraph/queries/delivery.graphql:1` | Studio `babysteps-sepolia@v0.1.0` 已部署并 100% 同步；从部署块 `11467677` 索引 9 个实体，真实 GraphQL 查询读回 2 个任务、2 笔购买和 1 张证书；证据见 `2026-08-12-the-graph-sepolia.json` | `complete` |
| AWS VPC/RDS/Lambda/CodeBuild 部署闭环，费用可控 | 已部署：隔离 VPC、2 AZ 私有子网、私有 RDS、5 分钟自动停库、Readiness Lambda、OIDC/S3/CodeBuild；明确延后：API Gateway、NAT/EIP、KMS、Secrets、生产 Relayer | `aws/pausable-template.yaml:46`<br>`aws/pausable-template.yaml:181`<br>`aws/pausable-template.yaml:225`<br>`aws/pausable-template.yaml:297`<br>`aws/pausable-template.yaml:357`<br>`aws/bootstrap.yaml:30`<br>`aws/bootstrap.yaml:129`<br>`aws/bootstrap.yaml:277`<br>`aws/buildspec.yml:16`<br>`scripts/validate-aws-readiness.mjs:6` | 批准的可暂停阶段 complete、生产阶段 deferred；43 项 AWS 测试、validator、两份 SAM lint 通过；Runtime `CREATE_COMPLETE`，CodeBuild `SUCCEEDED`，RDS `stopped`，无 IGW/NAT；见 `docs/evidence/deployment/2026-08-11-aws-pausable.md` | `partial` |
| 产品增强：可转送成长星兑换随机 StarBuddy 纪念卡，并允许同卡融合升级 | 固定 12 成长星抽卡；Chainlink VRF 独立随机四阶段和 70/22/7/1 稀有度；三张同系列同稀有度卡按 100/70/40% 融合；失败随机烧 1 张并解锁 2 张；24 小时超时退款/解锁且忽略迟到回调；卡片为 ERC-5192 SBT | `contracts/contracts/OnchainNotebook.sol`<br>`contracts/contracts/StarBuddyKeepsakes.sol`<br>`contracts/contracts/StarBuddyKeepsakeSBT.sol`<br>`contracts/scripts/runSepoliaStarBuddyClosedLoop.ts`<br>`web/src/features/keepsakes/KeepsakeGalleryPage.tsx`<br>`web/src/features/keepsakes/useKeepsakes.ts` | Sepolia 三合约已部署并完成 VRF consumer 配置；真实抽卡交易 `0x2b8c…a64` 铸出锁定 SBT #1（星耀 · 闪耀星宝），固定扣除 12 成长星且 URI 可公开读取；完整回执见 `docs/evidence/deployment/2026-08-14-starbuddy-sepolia-closed-loop.json`。真实融合仍等待自然积累三张匹配卡 | `partial` |
| 性能 SDK、AWS 日志接收、ECS 清洗与真实统计页 | SDK 采集五项 Web Vitals、资源/请求、JS error、custom/Web3 时长；采样、限流、20 条/5 秒批量、sendBeacon 优先与失败静默；Worker 隐藏 Origin Token；API 校验后按 SQS 10 条上限安全拆批进入 SQS/DLQ；一次性 ECS 脱敏、归一化，并用单条 SQL 按 event UUID 幂等写原始事件和小时聚合；Dashboard 只从聚合表回读，按时间/route/metric/env/version 筛选并显示真实 sampleCount、p50/p75/p95、错误率、趋势和 route 对比；取证后删除临时 Schema/Stack，公开页面不伪装成持续在线 AWS 服务 | `web/src/performance/client.ts`<br>`worker/src/performanceProxy.ts`<br>`aws/src/performance/ingestLambda.ts`<br>`aws/src/performance/sqsTransport.ts`<br>`aws/src/performance/cleanerMain.ts`<br>`aws/src/performance/storage.ts`<br>`aws/migrations/0002_performance.sql`<br>`web/src/pages/PerformanceDashboardPage.tsx`<br>`aws/performance-template.yaml`<br>`.github/workflows/aws-performance.yml` | 最终 Run `33160455921`（commit `e40008e056d2`）以本地 Chromium、Vite Preview 和本地 Worker 代理连接临时 AWS 后端，覆盖 5 条页面路径、25 个批次、415 个浏览器事件；本轮 ECS Cleaner 处理并写入 103 条，0 丢弃、0 可重试失败，清理前控制面仍显示 80 条 SQS 可见消息，因此不宣称全量队列排空。Live API 返回 LCP/FCP/TTFB、导航和脚本资源真实分位数，并生成 1440/390 双端截图与 4.84 秒录屏。随后 Schema 删除与不存在性验证通过，精确项目 Stack 删除，ECR/ECS/SQS/API/Lambda/Logs/Secret/SG/IAM 等 12 类项目资源全部为 0；共享 VPC/NAT/PostgreSQL/OIDC/Foundation 保持受保护。机器证据见 `docs/evidence/deployment/2026-08-28-performance-aws-final.json` | `complete` |

## 当前真实外部状态

- Ethereum Sepolia chain ID：`11155111`
- BabyCoin：`0x108a55217011983b93C3A95aD8D3B3343Bd5471b`
- GrowthActivities：`0x69935c3683eBbf34c898B8DC7404E546a7E1939a`
- GrowthCertificate V1：`0x4d594aeeAAfb4280D95CD60940AeBd3d11DBAFa3`
- TaskMarketplace V1：`0x2D1107610eBaBbFa7CD9569eb42eF315eb6F25BE`
- GrowthCertificate SBT V2：`0xF4efB99228f3ae6733d0c6CC5C5772bB5b37F654`
- TaskMarketplace V2：`0x2EE9fAFE99e143e5a1376805753D026bDac715de`
- V1 完整业务闭环：`docs/evidence/deployment/2026-08-09-business-closed-loop.json`
- V2 完整业务闭环：`docs/evidence/deployment/2026-08-11-sepolia-v2-business.json`
- Cloudflare D1：`babysteps-production`，远程 migrations `0001`–`0003` 已应用并核对表/索引
- Cloudflare Worker：`https://babysteps-api.baby2b.online`，版本 `1fa5eb56-57a6-4228-80f2-44403cecfd47`，100% 流量；HTTP/TLS/CORS 与完成路由鉴权边界已验证
- AWS Bootstrap：`babysteps-aws-readiness-bootstrap`，`UPDATE_COMPLETE`
- AWS Runtime：`babysteps-delivery-readiness`，`CREATE_COMPLETE`；RDS `stopped`
- Privy Dashboard 正式域名与生产 App ID 已配置；challenge-sign-verify、HttpOnly 会话、D1 用户名写入及 `PUT` CORS 修复已验证
- Subgraph Studio 已完成部署、同步和 GraphQL 读回；Infura 与 Alchemy 均为免费 Sepolia 专用配置，三源 RPC 已对照一致；IPFS pin 仍待外部完成证据
- StarBuddy Sepolia：OnchainNotebook `0xDA7A…CF91`、KeepsakeSBT `0xED65…E627`、Keepsakes `0xb343…bF68`；VRF consumer 和真实抽卡 SBT #1 已验证；真实融合等待自然积累三张匹配卡

## 更新规则

每个关键节点结束时更新本表。没有合约地址、交易哈希、部署 ID、HTTP/RPC/GraphQL 返回或云资源读取结果时，不把本地测试升级成外部 `complete`。第三方不可用或缺少授权时保留 `partial`，绝不以计划或截图替代运行证据。
